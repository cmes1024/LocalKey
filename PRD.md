# 🔒 LocalKey (Local-First Password Manager) 产品需求文档 (PRD)

## 一、 产品愿景与定位
一款基于 Chrome Manifest V3 标准开发的**纯本地、零知识架构**的密码管理插件。摒弃传统的 Popup 弹窗，采用**全局侧边栏 (Side Panel)** 交互设计，主打“伴随式”的安全密码管理体验。所有数据加密存储在本地，绝不上传云端，将数据主权完全交还给用户。

---

## 二、 核心功能模块 (Core Features)

### 1. 智能侧边栏 (Smart Side Panel)
*   **常驻陪伴**：在浏览器右侧独立开辟工作区，不遮挡主网页视线，点击网页空白处也不会消失。
*   **上下文感知 (Context-Aware)**：监听浏览器标签页 (Tab) 切换。当用户在左侧切换不同网站时，右侧侧边栏自动刷新，**优先置顶展示**当前匹配域名的账号列表。
*   **快捷填充与复制**：列表项提供“一键填充表单”、“一键复制账号”、“一键复制密码”三个快捷动作。

### 2. 零知识安全加密 (Zero-Knowledge Security)
*   **主密码保护**：初次使用需设置唯一主密码，插件内只保存主密码的哈希值用于验证。
*   **AES-256-GCM 强加密**：利用 Web Crypto API，通过主密码实时推导密钥。存入本地数据库 (`chrome.storage.local`) 的数据全部为密文，无明文泄露风险。
*   **内存即焚**：解密后的真实密码只存在于运行时的内存中，侧边栏关闭或长时间无操作后，立刻销毁内存中的明文数据，恢复锁定状态。

### 3. 数据生命周期管理
*   **增删改查**：提供完整的手动录入界面（域名、账号、密码、自定义备注文本）。
*   **明文/密文双轨导出**：
    *   *明文 CSV 导出*：方便用户迁移到其他主流密码管理器（需主密码二次验证）。
    *   *密文 JSON 导出*：生成高强度的加密备份文件，换电脑时导入该文件并输入原主密码即可完美恢复。

---

## 三、 💡 创意体验增强点 (Creative Additions)
为了让这个基础工具变得更专业、更好用，我们加入以下几个进阶/创意功能：

### ✨ 体验优化方向：
1.  **全局快捷键唤醒 (Global Hotkey)**：支持配置快捷键（如 `Mac: Cmd+Shift+P` / `Win: Ctrl+Shift+P`），无需移动鼠标，瞬间唤出或关闭侧边栏。
2.  **网站图标自动抓取 (Favicon Auto-fetch)**：列表展示时，根据存入的域名，自动抓取该网站的 Favicon 图标显示在账号旁边，做到“看图识账号”，大幅提升视觉检索效率。
3.  **沉浸式夜间模式 (Dark Mode)**：跟随系统或浏览器的主题，自动切换深色/浅色 UI，适合夜晚“暗中操作”。

### 🛡️ 安全增强方向：
4.  **密码健康度体检 (Password Health Check)**：在管理面板提供一个“体检”按钮，纯本地扫描所有密码。自动标记出：**重复使用的密码**（跨站撞库风险）和**弱密码**（如 `123456`），提醒用户修改。
5.  **原生密码生成器 (Password Generator)**：在侧边栏提供一个“骰子”图标。当用户在注册新网站时，一键生成带有大小写、数字、特殊符号的“高强度随机密码”，并直接保存。
6.  **防窥视与离开锁定 (Auto-Lock / Panic Mode)**：
    *   **空闲锁定**：可设置 5、15、30 分钟无操作后，侧边栏自动变回“需要输入主密码”的锁定状态。
    *   **模糊防窥**：在展示密码列表时，密码字段默认显示为 `******`，甚至可以加入选项让“账号”也做脱敏处理，鼠标悬停 (Hover) 才显示真实内容，防止背后有人偷看。

---

## 四、 技术架构设计 (Tech Stack)

*   **生态标准**：Chrome Extension Manifest V3
*   **入口模式**：`chrome.sidePanel` API
*   **权限需求**：`sidePanel`, `tabs`, `storage`, `activeTab`
*   **前端 UI**：HTML5 + TailwindCSS (轻量且现代化) + 原生 JavaScript (或引入轻量级框架如 Vue3/Alpine.js)。
*   **加密核心**：`window.crypto.subtle` (原生 Web 密码学 API)
*   **脚本通信**：`background.js` (负责状态管理和加密中枢) <-> `content.js` (负责注入到网页完成自动填充) <-> `sidepanel.js` (UI 交互逻辑)。

---

## 五、 开发实施计划 (Phased Roadmap)

*   **Phase 1: 核心链路打通 (MVP)**
    *   完成 Manifest 配置，成功调起 Side Panel。
    *   实现当前活动 Tab 域名的实时读取。
    *   跑通纯明文的 LocalStorage 存取和列表展示。
*   **Phase 2: 安全堡垒建设**
    *   引入主密码验证机制。
    *   实现密码库的加密存储和运行时解密。
    *   完成导入导出功能。
*   **Phase 3: 体验与创意落地**
    *   注入 Content Script 实现表单“一键自动填充”。
    *   上线网站图标获取、密码生成器、健康度体检等创意功能。
    *   UI 细节精调与动效优化。
