# 🎨 Chrome 密码管家 UI/UX 交互设计指南 (给 UI 绘制人员/工具的 Prompt)

这份文档旨在为 UI 绘制阶段（无论是提供给人类设计师还是 AI 绘图工具如 Pencil/Midjourney 等）提供详尽的页面结构、组件清单和交互状态说明。

## 📱 1. 全局设计规范 (Global Design System)
*   **承载容器**：Chrome Side Panel (侧边栏)，推荐画布宽度 `360px` - `400px`，高度自适应浏览器视口。
*   **设计风格**：Modern, Clean, 极简主义。建议采用微弱的毛玻璃效果 (Glassmorphism) 和大圆角 (8px - 12px)。
*   **色彩系统**：
    *   *主色调 (Primary)*：能够代表安全与信任的颜色（如克莱因蓝、深青色或紫色）。
    *   *辅助色*：成功（绿色）、警告/危险（红色）。
    *   *夜间模式 (Dark Mode)*：深灰背景 (`#1A1A1A` 或 `#121212`)，文字纯白或亮灰。
*   **排版 (Typography)**：清晰的无衬线字体（如 Inter, Roboto, SF Pro），注重字重对比（标题加粗，备注正文调浅）。

---

## 🖼️ 2. 核心页面拆解 (Screen by Screen)

### 页面一：解锁页 (Lock Screen)
**场景**：插件刚启动，或闲置超时被锁定后展示的页面。
*   **视觉重心**：居中对齐。
*   **页面元素**：
    1.  **Logo/Icon**：产品标志（例如一个带锁的盾牌或钥匙孔）。
    2.  **欢迎语**：大标题 "欢迎回来" (Welcome Back)。
    3.  **输入框**：主密码输入框（必须是密码模式 `***`，右侧带一个“闭眼/睁眼”的切换图标）。
    4.  **主按钮**：显眼的“解锁 (Unlock)”按钮。
*   **交互状态**：密码输入错误时，输入框边框变红，下方出现红色小字提示“主密码错误”。

### 页面二：主列表页 - 智能匹配 (Main Dashboard) 🌟核心页面
**场景**：解锁后默认进入的页面，优先展示与左侧网页相关的账号。
*   **顶部导航 (Header)**：
    1.  **搜索框**：带有放大镜 Icon，可以全局搜索所有账号。
    2.  **添加按钮**：右上角的 `+` 号 Icon。
    3.  **设置按钮**：右上角的 ⚙️ 齿轮 Icon。
*   **当前网站专属区 (Current Context)**（高亮/卡片化展示）：
    *   *小标题*：例如 "当前网站: github.com"（带一点呼吸灯或高亮动效）。
    *   *账号卡片 (Account Card)*：
        *   **左侧**：自动抓取的该网站 Favicon (小图标)。
        *   **中间**：上排显示 Username (加粗)，下排显示 Password (默认显示为 `••••••••`)。
        *   **悬浮交互 (Hover)**：鼠标移到密码上方时，`••••••••` 变成真实的明文密码。
        *   **右侧操作区**：三个纵向或横向排列的小图标按钮：`[复制账号]`、`[复制密码]`、`[一键填充 (火箭/闪电图标)]`。
*   **所有账号区 (All Accounts)**（下方列表）：
    *   一个可滚动的列表，展示其他网站的密码。设计上比“当前网站”的卡片稍微扁平/暗淡一些，以突出主次。

### 页面三：添加/编辑表单 (Add/Edit Entry)
**场景**：用户点击 `+` 号后进入的录入界面。
*   **页面元素 (表单区)**：
    1.  **网址输入框 (URL)**：自动带入当前页面的网址。
    2.  **账号输入框 (Username)**。
    3.  **密码输入框 (Password)**：
        *   右侧内嵌一个 **🎲 骰子图标 (密码生成器)**。
        *   点击骰子后，输入框下方展开一个面板，包含一个滑动条（控制密码长度 8-32），以及三个 Checkbox（包含数字、包含符号、包含大写）。
    4.  **备注文本域 (Notes)**：可写多行文字的多行文本框。
*   **底部操作区**：
    *   取消 (Cancel) 按钮（弱色/边框状）。
    *   保存 (Save) 按钮（主色/实心填充）。

### 页面四：设置面板 (Settings)
**场景**：侧边栏内的抽屉式菜单或独立页。
*   **区块 1: 安全设置**
    *   修改主密码 (按钮/入口)。
    *   空闲自动锁定时间（下拉菜单：5分钟/15分钟/30分钟/从不）。
*   **区块 2: 密码体检中心 (Health Check)**
    *   一个显眼的进度条或评分环（例如：安全评分 85/100）。
    *   下方列出风险项：[3 个重复使用的密码] ⚠️、[1 个弱密码] ❌。
*   **区块 3: 数据管理**
    *   按钮组：`[导入 CSV/JSON]`、`[导出明文备份(红色警告图标)]`、`[导出加密备份]`。

---

## 👆 3. 关键微交互细节 (Micro-Interactions) 给动效/UI设计的提示
1.  **复制成功反馈**：点击“复制”Icon 时，Icon 瞬间变成一个绿色的“✅ 对勾”，并伴随轻微的缩放动画 (Scale)，2秒后恢复原样。
2.  **一键填充反馈**：点击“填充”按钮后，按钮周围产生一圈水波纹扩散效果 (Ripple effect)，表示数据已发射到左侧网页。
3.  **防窥视模糊**：在列表页，如果开启了“防窥模式”，账号卡片的文字可以加上 CSS 的 `blur()` 滤镜，鼠标移入时 `blur(0)` 清晰显示。
